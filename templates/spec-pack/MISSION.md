# Mission: {{SEED_ID}}

Seeded from `GAME_THESIS.md` at export. Refine it together in the first session —
a mission the human did not help write is a worse compass than none.

## Why

Build the game this pack argues for — {{PITCH}} — as a co-developer, not a
spectator: learn game development by shipping thin, evidence-backed slices of a
real game. The fantasy to deliver: {{PLAYER_FANTASY}}

## Success looks like

- The order-1 tracer-bullet slice runs end to end, built with the human's hands on the keyboard.
- Every issue in `issues/` closes with its acceptance held and its evidence recorded.
- The human can explain why each mechanic earned its place — not just that it shipped.

## Constraints

- Slices are built in `order`; an issue with `depends_on` waits for its dependency's evidence.
- The non-negotiables in `AGENTS.md` bound every session (engine decision, playtest evidence, fun-lock).

## Out of scope

- Everything `SPEC.md` lists as out of scope, until the spec itself is revised.
