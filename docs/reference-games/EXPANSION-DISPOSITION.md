> Promoted from `.scratch/orchestrate/fue/` (run ephemera) into docs/ for git durability
> (Shane-approved housekeeping follow-up, 2026-07-14). This copy is canonical.

# Canon expansion — trinity council disposition (2026-07-12)

Blind consults on CANON-EXPANSION-PROPOSAL.md: expansion-consult-grok.log +
expansion-consult-codex.md. Both NOT READY; strongly convergent. Orchestrator
dispositions (Shane mandated council-decides-operationalization):

## Adopted (convergent)
1. Two-tier index: Tier-1 CANON full cards / Tier-2 genre-index rows.
2. Tier-2 citable at intake as vocabulary ONLY; P07/P18 require audited Tier-1.
3. Dual taxonomy, design facets primary: `design_shape` (register, loop-class,
   session structure, progression form, player count) is the ontology the
   critic runs on; `market_genres` (storefront-derived, versioned) is coverage/
   discovery evidence only. Taxonomy v1 FROZEN before fill (circularity guard).
4. Hard typed evidence per row: {claim, metric_type, value_or_range, class,
   class_definition (versioned thresholds), platform, territory, observed_at,
   source, source_tier} — raw observations preserved; no freehand classes;
   no from-memory rows.
5. Pilot first: ~20 games across 4-5 design facets; freeze taxonomy +
   thresholds; two consecutive clean completeness-critic passes (critic scope:
   facet floor <3, hybrid facets lacking cross-genre, 10-20% citation
   spot-audit) → then fill to floors. Target 72-90 unique games total; stop
   at diminishing value, never chart completeness.
6. One game = one row; multi-bucket membership fields (primary + secondary);
   coverage counts generated, never duplicated rows.
7. Moat rule: ≤120 chars, must name a MECHANISM (not genre/aesthetic).
8. fun_per_budget_note CUT → `scope_efficiency_hypothesis` + optional sourced
   production-scale evidence + `exemplar_rationale`; unknown budget stays
   unknown.
9. Promotion: triggered AT INTAKE when a seed names a Tier-2 nearest
   reference; full card + audit completes before thesis compilation or the
   reference is dropped for that run. Promotion queue derived from live
   intake usage; `promotion_candidate` flag CUT.
10. Shane ratifies taxonomy v1 + promotions; never individual rows.
11. Tier-1 audit hardening: per-claim citation coverage (moat, each depth
    mechanism, feel signature, packaging lessons, material BOM claims) — the
    16-card fan-out prompts adopt this NOW (schema min stays; audit protocol
    demands more).
12. Naming: the artifact set is the "reference canon"; never abbreviate to
    design_canon (existing doctrine term for cloneable design-system repos).
13. Cut: chart-top-50 gate, per-genre hard cap 8, quarterly refresh/cron,
    150-row ambition, duplicated cross-genre rows, uncited vibes fields.

## Sequencing (adopted from both)
Wave 3 (this run): author + cross-audit the 16 Tier-1 cards (per-claim
citations), materialize CANON.md. THEN Tier-2: schema
(`genre-index-row` + TAXONOMY.md) and ~20-game pilot as a follow-up mission
under this disposition; fill-to-floors only after the pilot's critic passes
and Shane ratifies taxonomy v1.

## Verdict
Operationalization RATIFIED at council level (READY for Tier-1 fan-out +
Tier-2 pilot); fleet sweep remains gated on pilot evidence.
