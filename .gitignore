# Dependencies
node_modules/

# Env and local
.env
.env.*
!.env.example
.factory/local_tool_probe.json
*.local
*.local.json

# Agent harness (local only)
.claude/
.omx/
.sandcastle/

# OS junk
.DS_Store

# Per-run state and research runtime (durable on disk, not factory source).
# A completed run's evidence lives in .tgf/seeds/<id>/, the exported spec pack /
# downstream co-dev repo's own git, and committed handoff docs — not the
# meta-factory's history.
.tgf/

# Python / build artifacts
__pycache__/
*.py[cod]
*.pyo
dist/
build/

# Obsidian graph junk
.obsidian/graph.json
.obsidian/workspace.json

# Godot export artifacts (downstream games)
.godot/
export/
*.pck
